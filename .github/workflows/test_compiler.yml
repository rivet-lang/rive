name: Test compiler

on:
  push:
    paths: ['lib/**.ri', 'src-self-hosted/**.ri', 'src/**.py', 'tests/**.ri']
    branches: [main]

jobs:
  # Linux
  ubuntu-clang:
    runs-on: ubuntu-latest
    env:
      RIVET_CC_TEST: clang
    steps:
        uses: actions/checkout@v2

      - name: Run tests
        run: |
          python3 tests/run_ok_tests.py

      - name: Run failing tests
        run: |
          python3 tests/run_failing_tests.py

  ubuntu-gcc:
    runs-on: ubuntu-latest
    env:
      RIVET_CC_TEST: gcc
    steps:
        uses: actions/checkout@v2

      - name: Run tests
        run: |
          python3 tests/run_ok_tests.py

      - name: Run failing tests
        run: |
          python3 tests/run_failing_tests.py

  # Windows
  windows-msvc:
    runs-on: windows-2019
    env:
      RIVET_CC_TEST: msvc
    steps:
      - uses: actions/checkout@v2

      - name: Run tests
        run: |
          python3 tests/run_ok_tests.py

      - name: Run failing tests
        run: |
          python3 tests/run_failing_tests.py

  windows-gcc:
    runs-on: windows-2019
    env:
      RIVET_CC_TEST: gcc
    steps:
      - uses: actions/checkout@v2

      - name: Run tests
        run: |
          python3 tests/run_ok_tests.py

      - name: Run failing tests
        run: |
          python3 tests/run_failing_tests.py
