import std/console;

#[inline]
proc factorial(n: uint32) -> uint32 {
	return if n <= 1 {
		1
	} else {
        n * factorial(n - 1)
	};
}

proc main() {
	res := factorial(5);
	console.writeln("factorial(5): {}", res);
	@assert(res == 120);
}
