fn main() void {
    // Rivet only has 1 way to write loops: `while`, which always executes,
    // until the given condition is false.

    let mut x = 0;
    while {
        x++;
        if (x < 5) {
            break;
        }
    }

    while (x < 10) {
        x++;
    }
}
