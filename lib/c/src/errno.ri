// Copyright (C) 2022 The Rivet Developers. All rights reserved.
// Use of this source code is governed by an MIT license that can
// be found in the LICENSE file.

import "c/libc";

public class ErrnoError : Error {
    public code: i32;
}

#[inline]
public fn errno() i32 {
    return unsafe {
#if _LINUX_
        libc.__errno_location().?.*
#else
        libc._errno().*
#endif
    };
}

public fn errno_msg(code: i32 = errno()) string {
    unsafe {
        let s = libc.strerror(code);
        return if s is nil { "" } else { string.from_raw(s) };
    }
}

#[inline]
public fn last_errno_error() ErrnoError {
    return ErrnoError(errno_msg(), errno());
}
