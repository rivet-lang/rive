import { Project, Version, Dependency } from rivet/build;

var project = Project(
    name: "core",
    description: "The Rivet core library",
    version: Version.build(0, 1, 0),
    authors: ["The Rivet Developers"],
    dependencies: [
        Dependency(name: "libc")
    ]
);
