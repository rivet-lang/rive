// Copyright (C) 2022 The Rivet Team. All rights reserved.
// Use of this source code is governed by an MIT license
// that can be found in the LICENSE file.

extern (C) {
    pub let environ: **u8;

	pub func access(a: *u8, o: i32) i32;

    pub func sysconf(name: i32) i64;

    pub func getpid() u32;
    pub func getppid() u32;

    pub func isatty(fd: i32) i32;

    pub func write(fd: i32, buf: *void, count: usize) isize;
    pub func read(fildes: i32, buf: *mut void, nbyte: usize) isize;
}
