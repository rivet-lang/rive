import { Project, Version, Dependency } from "rivet/build";

var project = Project(
    name: "std",
    description: "The Rivet standard library",
    version: Version.build(0, 1, 0),
    authors: ["The Rivet Developer"],
    dependencies: [
        Dependency(name: "runtime")
    ]
);
