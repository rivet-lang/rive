// Copyright (C) 2022 The Rivet Developers. All rights reserved.
// Use of this source code is governed by an MIT license that can
// be found in the LICENSE file.

import "runtime" {
    pub mem_alloc as alloc,
    pub mem_zeroed as zeroed,
    pub mem_resize as resize,
    pub mem_set as set,
    pub mem_copy as copy,
    pub mem_move as move,
    pub mem_dup as dup,
    pub mem_cmp as cmp,
    pub mem_dealloc as dealloc
};
