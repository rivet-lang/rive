/*
* Multi-comment
*
* /*
* * Nested comment
* */
*/

extern pkg core;
extern pkg std;

extern "C" {
    fn exit(status: i32);
    fn abort();
}
extern "C" fn x();

mod real {
    @[abstract; virtual; unsafe]
    pub fn scrash() {}

    pub mod sis {
        fn inner(a: &[i32], mut b: [u8; 4]) {
        }
    }

    fn x() !i32 {
    }

    fn z() ?bool {}
}

/// DOC-COMMENT
fn main() {
    // simple comment
}
