import "std/console";

func factorial(n: uint32) uint32 {
	if n <= 1 {
		return 1;
	}
	return n * factorial(n - 1);
}

func main() {
	let res = factorial(5);
	console.println("factorial(5): {}", res);
	@assert(res == 120);
}
