proc main() {
    x := 1;
    x := 1; // FAIL

    y := 2;
    {
        y := 5; // disallow variable shadowing
    }
}

proc other(a: int32) {
    a := 1;
}

proc other(a: int32) { // FAIL
    a := 1;
}

struct Gota { }
struct Gota { } // FAIL

enum State {
    Idle,
    Idle // FAIL
}
