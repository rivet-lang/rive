fn make_array() [i32; 4] { return [1, 2, 3, 4]; }

fn main() {
    let lit = [1, 2, 3, 4];
    assert!(lit == [1, 2, 3, 4]);
    assert!(lit.len == 4);

    let arr = make_array();
    assert!(arr == [1, 2, 3, 4]);
    assert!(arr.len == 4);
}
