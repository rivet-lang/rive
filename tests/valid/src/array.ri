fn new_array() [i32; 4] {
    return [1, 2, 3, 4];
}

test "array literal" {
    var lit = [1, 2, 3, 4];
    assert!(lit == [1, 2, 3, 4]);
    assert!(lit.len == 4);
}

test "array returned from function" {
    var arr = new_array();
    assert!(arr == [1, 2, 3, 4]);
    assert!(arr.len == 4);
}
