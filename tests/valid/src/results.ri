fn result() !void {
    return Error("simple error");
}

fn result2() !void {
    return Error("simple error (returnd: {}, idx: {})", true, 0);
}

test "catch result error" {
    result() catch |err| {
        assert!(err.msg == "simple error");
    };

    result2() catch |err2| {
        assert!(err2.msg == "simple error (returnd: true, idx: 0)");
    };
}
