test "while statement" {
    let mut x = 0;
    while { // infinite loop
        x++;
        if (x == 5) {
            break;
        }
    }
    assert!(x == 5);

    while (x < 10) {
        x++;
    }
    assert!(x == 10);
}
